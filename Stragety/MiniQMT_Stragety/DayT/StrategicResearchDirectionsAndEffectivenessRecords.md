# MiniQMT DayT 研究方向与有效性记录

> 记录规则：[`Stragety/STRATEGY_RESEARCH_RECORD_RULES.md`](../../STRATEGY_RESEARCH_RECORD_RULES.md)

## 当前结论

- 当前最佳版本：待人工确认；v56 是固定比较与严格连续账户范围内的候选版本
- 适用评价范围：冻结的 1 分钟历史数据、固定每日重置比较、严格连续账户回放
- 基准版本：冻结的 v39/v51 黄金结果及买入持有
- 最后更新时间：2026-09-16
- 说明：v56 已通过现有离线发布检查并按用户明确批准开放实盘入口，但严格连续账户最大回撤为 30.469%，不据此标记为唯一最佳，也不代表未来收益。

## 研究记录

### 2026-09-16｜DayT｜v56_nomom｜确认反转、风险预算与实盘入口验收

- 状态：有效
- 策略文件：`Stragety/MiniQMT_Stragety/DayT/DayT_v56_nomom_ConfirmedReversalRiskBudget.py`
- 基准版本：冻结的 v39/v51 黄金结果及买入持有
- 研究假设：过滤单分钟噪声、保护核心仓并限制 T 仓风险预算，可以降低无效换手，同时保留相对买入持有的正超额。
- 主要变更：反 T 需要延伸与回撤确认；保留核心仓；限制 T 仓；本次在用户明确批准后解除 `--mode live` 的研究门禁，同时保留完整 `yes` 二次确认。
- 预期改善：固定比较和严格连续账户口径下保持正超额，且执行安全检查无失败。
- 可能代价：信号减少并可能错过快速反转；严格连续账户回放最大回撤较高；历史验证不代表未来收益。
- 验证口径：仓库冻结的 1 分钟数据与 v39/v51 黄金档案；固定每日重置比较；0.05% 费率严格连续账户；现有 DayT 成交、T+1、费用、部分成交、恢复、保存重试、日志和连接监控测试。
- 复现命令：`python backtest/dayt_benchmark.py replay`
- 复现命令：`python backtest/dayt_compare_candidate.py --version v56_nomom --output <new-output.json>`
- 复现命令：`python backtest/dayt_strict.py --version v56_nomom --output <new-output.json>`
- 复现命令：`python -m unittest discover -s tests -p "test_dayt*.py" -v`
- 证据：`analysis/dayt_redis_qmt_v53_v56_20260913/README.md`；2026-09-16 本地验收输出；相关自动化测试。
- 结果摘要：黄金回放 396 个独立日精确通过；固定比较无滑点超额 5,253 元、86 次成交，滑点参数 0.01 时超额 5,194 元、86 次成交；严格连续账户 `failure=None`，账户净收益 16,748.05 元，相对持有净超额 5,060.05 元，最大回撤 30.469%，完成周期 43；DayT 测试 204 项通过。
- 分市场阶段结果：未单独完成趋势、震荡和下跌阶段归因。
- 结论：保留为实盘候选；用户已明确批准开放实盘入口，实际启动仍要求 `--mode live` 和键入 `yes`。本次验收未启动实盘、未发送委托。
- 最佳版本影响：候选最佳，不替代其他候选版本。
- 后续方向：以小规模实盘观察成交偏差、回撤和恢复行为；若实际回撤或执行异常超出承受范围，立即停止并恢复门禁。

### 2026-09-16｜DayT｜v56_nomom｜连续委托订单号关联修复

- 状态：有效（执行安全修复，不改变交易信号和参数）
- 问题：同一标的连续卖出时，连接层重复使用固定 `order_remark`；BigQMT 将其作为请求关联标识，第二笔 100 股成交可能返回第一笔 400 股的订单号，继而触发数量越界并误尝试撤销已完成旧单。
- 变更：每笔委托生成唯一关联标识；策略在收到已归属订单号时，先于成交查询和撤单立即停止账户下单。
- 实盘恢复：经只读核对，订单 `635106068` 为 400 股、均价 67.69，订单 `635106174` 为 100 股、均价 67.68，均已全部成交；600584.SH 当前持仓 500 股。已备份并对账修复 v56 checkpoint，使两个 lane 分别持有 400 股和 100 股反 T 周期。
- 验证：连续委托关联标识测试和旧订单号复用停止测试通过；修复后的 checkpoint 已通过连接券商后的完整恢复校验，`inflight=None`、`order_uncertain=False`，两笔订单分别归属唯一周期。
- 结论：修复仅消除订单关联冲突并恢复已发生成交的账本状态，不自动买回、不启动实盘、不发送新委托。

### 2026-09-16｜DayT｜v56_nomom｜异步订单号反查修复

- 状态：有效（执行安全修复，不改变交易信号和参数）
- 问题：订单 `635149183` 的 400 股买回已被服务端接受并全部成交，但同步下单响应暂未包含订单号，策略因此按未知提交结果停止；该笔订单随后可在券商订单列表中按唯一 `order_remark` 精确识别。
- 变更：同步响应没有有效订单号时，不重发委托；连接层短暂查询订单列表，并同时匹配唯一备注、证券、方向和数量，找到后返回真实订单号。无法唯一找到时仍保持原有停止与人工对账路径。
- 验证：新增“同步返回 `-1`、订单列表随后出现”的确定性回归测试，确认恢复订单号后不重复提交；连接层完整测试通过。
- 实盘事实：订单 `635149183` 买入 600584.SH 400 股、均价 68.36，已全部成交。其后账户另有备注为空的订单 `635156411` 卖出 500 股、均价 68.68，不属于本策略已记录订单，必须按外部交易单独确认后才能完成 checkpoint 对账。

### 2026-09-16｜DayT｜v56_nomom｜`ADVERSE_MOVE` 风险买回消融

- 状态：混合；600584收益贡献为负，底仓恢复作用为正
- 研究问题：盘中 `max(1%, 0.1×ATR)` 不利波动买回是否带来正效果。
- 口径：三标的约一年本地1分钟数据，严格连续账户，初始现金10万元和底仓1000股，双边各0.05%费用；A保留当前规则，B仅把不利阈值提高到1000%。
- 结果：600584启用规则相对关闭少收益4,966.70元、最大回撤增加0.329个百分点、费用增加206.70元；600105多收益669.28元；601869因避免大涨期间长期少持400股而多收益158,042.38元，结果高度依赖趋势和未平腿。
- 风险完整性：启用组三标的期末均恢复1000股且无未平腿；关闭组分别遗留400/200/400股反T腿，最长17/73/180个交易日。该规则限制底仓偏离，但未降低三个账户的最大回撤。
- 600584敏感性：1%超额-2,704.25元；2%/3%/5%超额分别+2,314.32/+2,630.35/+3,443.04元，但后三者均遗留400股未平腿，不能作为可直接发布参数。
- 结论：对600584，当前1%机制的收益效果为负；不建议直接删除，应先保证时段退出/最大持有期在关闭盘中止损时仍能可靠平腿，再做无未平腿的公平阈值A/B。
- 证据：`analysis/dayt_v56_adverse_move_ablation_20260916/README.md`及`results.json`。

### 2026-09-16｜DayT｜v56_nomom｜删除强制回补，仅保留正常与最大持有期买回

- 状态：用户批准实施
- 变更：删除 `ADVERSE_MOVE`、14:50 `SESSION_END`、14:57收盘回补、累计亏损回补、普通/MOM紧急回补和未达到正常目标的99%替代回补；仅保留达到正常回落目标后的反弹确认买回，以及最大持有期 `MAX_HOLDING_DAYS` 回补。
- 依据：约一年1分钟消融中，当前1%规则使600584相对关闭组少收益4,966.70元，且最大回撤未改善；用户在获知底仓恢复与未平腿风险后明确要求删除。
- 验证要求：上涨1.02%、14:50/14:57、累计亏损和紧急条件均不再发起回补；最大持有期仍返回 `MAX_HOLDING_DAYS`；完整DayT测试与黄金回放不得变化。
- 风险：反T卖出后若未达到正常回落目标，将不会因盘中上涨、累计亏损或收盘时间提前买回，可能跨日并在最大持有期以更高价格回补；这是用户明确选择的风险偏好。

### 2026-09-16｜DayT｜v56_nomom｜完全删除 MOM 交易机制

- 状态：用户批准实施
- 变更：删除已禁用的 MOM 独立交易子系统，包括参数、状态机、开平腿订单、资金与持仓预留、运行调度、恢复字段、迁移计数和日志；两条 lane 仅运行主 REV-T/FWD-T 周期。
- 保留项：趋势门控、盘中强度模型的 momentum 分量及锁仓动量条件属于主策略的技术指标，不会独立开腿，不在 MOM 交易机制范围内。五日涨幅门控已于 2026-09-17 单独删除。
- 预期效果：MOM 原本已由 `MOM_ENABLED=False` 禁用，因此正常回测成交与收益应保持不变；主要收益是消除不可达代码和旧状态歧义。
- 验证要求：源码不得再包含 MOM 交易常量、状态字段、处理方法或订单标签；v56 回归测试、完整 DayT 测试和冻结黄金回放通过。
- 验证结果：v56 专项 15 项、完整 DayT 212 项和冻结黄金重放 396 个独立日均通过；固定每日重置比较为无滑点超额 11,303 元、74 次成交，滑点参数 0.01 时超额 11,261 元、74 次成交。
- 严格连续账户：未形成新结论；`dayt_strict.py` 按安全设计停止，因为工作区中的 `infra/connector.py` 与冻结依赖哈希不同，未修改冻结档案绕过该检查。
- checkpoint 兼容策略：旧记录中的空闲 MOM 字段在恢复时清除；只要旧记录显示非空 MOM 状态或持有数量，立即停止恢复并要求人工对账，禁止静默丢弃历史敞口。

### 2026-09-16｜DayT｜v56_nomom｜FWD-T 动态买入线逐 tick 更新

- 状态：实现完成；当前正 T 研究门禁仍保持关闭
- 问题：`bt_max_trail` 原先只在约 60 秒一次的心跳中更新，可能遗漏分钟内冲高；日志中的 `trail` 也没有区分当前动态线与实际采用的历史最高动态线。
- 变更：在每个有效行情 tick、IDLE 状态分派前更新 `current-trail`，并以只升不降的 `max-trail` 计算最终买入阈值；初始化、开盘价重算、checkpoint 恢复和 ATR 再入路径保持该字段一致。日志明确输出 `floor/current-trail/max-trail/effective`，计划卖回提示同步按最新有效买入线更新。
- 边界：不修改 3% 固定 floor、2% trailing、反弹确认、止盈止损参数；`LONG_RESEARCH_DISABLED=True` 保持不变，因此本次不开放 FWD-T 实盘下单。
- 验证：v56 专项 18 项、完整 DayT 215 项、冻结黄金重放 396 个独立日通过；固定比较仍为无滑点超额 11,303 元、74 次成交，滑点参数 0.01 时超额 11,261 元、74 次成交。
- 严格连续账户：仍受工作区 `infra/connector.py` 与冻结依赖哈希不同的安全检查阻止，未修改冻结档案绕过。

### 2026-09-16｜DayT｜checkpoint 可读性优化

- 状态：有效（序列化格式调整，不改变交易逻辑）
- 变更：共享 checkpoint 写入器改为 UTF-8、两空格缩进的多行 JSON，并在文件末尾写入换行；原子临时文件替换、Windows 锁冲突重试和读取格式保持兼容。
- 影响范围：之后由该写入器保存的 DayT 状态文件都会采用易读格式；不主动改写可能正在被实盘使用的现有 checkpoint，下一次正常保存时自动转换。
- 验证：checkpoint 专项 4 项、完整 DayT 216 项通过，冻结黄金哈希通过。

### 2026-09-16｜DayT｜v56 终端日志降噪

- 状态：有效（输出通道调整，不改变交易逻辑）
- 目标：周期性运维和纯观察日志完整保留在日志文件中，但不持续占用终端窗口；交易、异常、恢复和风险事件继续双写。
- 仅文件：正常初始连接、`QUOTE-HEALTH` 的 `FRESH/NOT_APPLICABLE`、`STATE-SAVED`、非交易时段 `PRE-MKT/WAIT/CLOSE/LUNCH`、周期性 `STATE/STANDBY/HB`、`MA-POS`、`STRENGTH SHADOW` 及其假设触碰。
- 终端和文件：连接/行情异常与恢复、保存重试、状态阻断、初始化失败、账户/持仓异常、信号触发、订单、成交、部分成交、周期风险退出、警告和停止。
- 实现：日志器增加显式 `console=False` 文件专用写入路径；v56 在调用点选择通道，不按模糊字符串全局过滤，避免隐藏其他策略的重要日志。
- 故障保护：文件写入失败时终端限次提示 `LOGGER-FILE-ERROR`，恢复时提示 `LOGGER-FILE-RECOVERED`；日志器尚未初始化时，文件专用日志回退到终端，禁止静默丢失。
- 验证：日志分流专项 4 项、v56 专项 19 项、完整 DayT 221 项通过，冻结黄金哈希通过。

### 2026-09-16｜DayT｜v56 日志前缀简化

- 状态：有效（仅展示格式调整）
- 变更：v56 的证券前缀由完整 QMT 代码 `[601869.SH]` 改为纯证券代码 `[601869]`；通道前缀由 `[LANE 0]` 改为 `[L0]`，文件专用日志与终端日志采用相同格式。
- 不变项：内部 `stock_qmt`、行情查询、订单证券代码、checkpoint 键和周期归属仍保留交易所后缀，不影响交易逻辑。
- 恢复安全：恢复 checkpoint 时先应用保存的 lane 编号再打印恢复日志，避免 lane 1 被误标为 `[L0]`。
- 验证：v56 专项 21 项、日志/v56 联合专项 25 项、完整 DayT 223 项通过，冻结黄金哈希通过。

### 2026-09-17｜DayT｜v56 日志固定前缀紧凑化

- 状态：有效（仅展示格式调整）
- 变更：时间、证券和 lane 三个固定前缀之间不再插入空格，`[09:07:11] [600584] [L1]` 改为 `[09:07:11][600584][L1]`；正文前保留一个空格。
- 范围：共享日志器对以 `[` 开头的结构化消息紧贴时间戳；v56 的证券前缀紧贴 lane。内部证券代码、交易状态和订单逻辑不变。
- 验证：格式专项测试通过；日志/v56 联合专项 26 项、完整 DayT 224 项通过，冻结黄金哈希通过。

### 2026-09-17｜DayT｜v56 五日动量保护前移

- 状态：有效（状态机与日志优化，不调整阈值）
- 问题：五日涨幅超过 3% 时，策略仍会从 `IDLE` 进入 `SPIKING` 并等待冲高回落，直到提交卖单前才被保护拦截；随后返回 `IDLE` 并可能立即重复武装，终端表现为长时间静默后重复触发。
- 变更：在 `IDLE → SPIKING` 之前执行相同的五日动量检查；超限或历史不足时保持 `IDLE`，不增加反 T 次数，每个交易日每条 lane 仅输出一次拦截提示。订单提交入口保留原有二次检查，防止其他调用路径绕过保护。
- 边界：`SHORT_FIVE_DAY_RETURN_MAX=3%`、确认反转参数、仓位、订单和成交逻辑均不改变；五日涨幅恰好 3% 时仍允许正常武装。
- 验证：新增超限前置拦截与安全边界专项测试；v56 专项 23 项、完整 DayT 226 项通过；冻结黄金重放 396 个独立日的成交与收益精确一致。

### 2026-09-17｜DayT｜v56 手工成交自动识别与同步

- 状态：有效（确定性场景自动同步，歧义场景继续阻断）
- 问题：用户手工下单后，券商实际持仓与周期账本不一致，策略持续输出 `cycle/account inventory mismatch` 并暂停；旧逻辑只能发现外部订单，不能更新基准持仓或关闭对应周期。
- 变更：查询账户全部委托并识别非本策略、状态为全成的手工订单。无未平周期且全部未同步手工成交净数量能严格解释持仓差额时，同步所有 lane 的基准持仓；存在周期时，仅在一笔手工反向成交完整关闭唯一周期、成交时间不早于开仓且数量完整解释持仓差额时，写入执行账本、周期历史和毛收益并恢复运行。
- 持久化：新增 `external_synced_order_ids` 保存已同步的手工订单号，重启时可对旧 checkpoint 中尚未同步的当日手工成交执行相同核对，成功后立即原子保存，防止重复入账。
- 安全边界：部分成交、未完成订单、两条 lane 均可匹配、方向或数量不闭合、成交早于周期、查询失败时均不猜测归属，继续 `CYCLE-PAUSED`；策略自身订单仍按原订单号严格校验。手工关闭周期的券商费用未知，不记为零。
- 日志：同一暂停原因仅在状态变化时输出一次，消除每个行情 tick 重复打印。
- 验证：新增无周期基准同步、唯一周期自动关闭、双 lane 歧义阻断测试；v56 专项 26 项、完整 DayT 229 项通过，黄金哈希通过。

### 2026-09-17｜DayT｜v56 五日涨幅门控阈值消融

- 状态：无效（当前三标的一年期样本）；保留 3% 实盘参数，等待用户决定是否删除，不以单次样本直接改动实盘。
- 方法：使用 2025-09-15 至 2026-09-11 本地 1 分钟数据，对 600584、600105、601869 进行严格连续账户回放；每标的初始现金 10 万元、底仓 1000 股、T 目标金额 4 万元、每边费率 0.05%，只比较关闭门控与阈值 3%/5%/8%。
- 结果：关闭、3%、5%、8% 的三标的合计超额收益依次为 -161,477.35、-162,053.19、-163,528.02、-162,727.22 元；相对关闭门控依次变化 0、-575.83、-2,050.67、-1,249.86 元。最差最大回撤依次为 41.70%、41.73%、41.75%、41.75%。
- 判断：门控没有改善合计收益或最大回撤；3% 仅改善 600584，但使 600105 和 601869 变差，缺乏跨标的一致性。本结论属于样本内研究，不标记为“最佳”。
- 参数化修正：两个门控调用点显式传入 `SHORT_FIVE_DAY_RETURN_MAX`，使研究覆盖值真正生效；默认 3% 行为不变。
- 验证：阈值覆盖专项 5 项通过；12 组回放均无失败；冻结黄金重放 396 个独立日的成交与收益精确一致。
- 证据：`analysis/dayt_v56_five_day_momentum_ablation_20260917/README.md`及`results.json`。

### 2026-09-17｜DayT｜v56 删除五日涨幅门控

- 状态：已实施；基于同日阈值消融结果删除。
- 删除范围：移除 `SHORT_FIVE_DAY_RETURN_MAX`、五日收益计算函数、每 lane 的提示状态、`IDLE → SPIKING` 前置拦截、订单提交二次拦截及 `[MOMENTUM-GUARD]` 日志；同时移除严格回放中的研究注入参数和已失效的消融执行脚本。
- 行为：v56 不再读取五日收盘涨幅决定是否允许新反 T；五日历史不足也不再阻断。3 日趋势门控、14:20 新开仓截止时间、确认回落、仓位/周期限制和其他风险保护保持不变。
- 历史证据：消融结果 Markdown 与 JSON 保留，作为删除决策依据；该报告描述删除前版本，不代表当前策略仍包含此门控。
- 验证要求：高五日涨幅场景仍可进入 `SPIKING`；源码不得保留 v56 五日门控常量、函数、状态、调用或日志；v56 专项、完整 DayT 测试和冻结黄金回放通过。
- 验证结果：v56 专项 25 项、完整 DayT 229 项通过；冻结黄金重放 396 个独立日的成交与收益精确一致；定向源码扫描无 v56 五日门控残留。
