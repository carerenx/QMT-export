# 本次无MOM回测结果

仅 `verified/` 是修复v39副本加载及跨日识别后、同一引擎完整重跑的正式结果。
根目录的 `v39_5bp.json` 属于首次试跑；随后v39无MOM加载未转换为分钟循环，已停止试跑，不将其用于对照报告。

`verified/` 中：无 `_nomom` 的文件是本次重新运行的MOM开启对照；含 `_nomom` 的文件是关闭MOM后的完整现金/持仓路径。
所有文件使用固定99个交易日的1分钟K线。正式报告见 `../DayT_no_MOM_1min_comparison.md`。
原始黄金结果、历史报告、实盘策略与账户检查点不覆盖。
