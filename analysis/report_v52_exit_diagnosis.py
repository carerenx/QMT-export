"""Evidence report and per-cycle/order appendices; proposes, never changes, rules."""
import json
from pathlib import Path
import pandas as pd

ROOT = Path(__file__).resolve().parents[1]


def main():
    result = json.loads((ROOT / 'analysis/nomom_20260914/verified/v52_nomom_5bp.json').read_text(encoding='utf-8'))
    trace = json.loads((ROOT / 'analysis/v52_nomom_trace_20260915/trace.json').read_text(encoding='utf-8'))
    assert trace['identical_replay']
    minute = pd.read_csv(ROOT / 'analysis/v51_v39_minute/1m.csv', dtype={'time': str})
    minute['stamp'] = pd.to_datetime(minute.time, format='%Y%m%d%H%M%S')
    history = result['owned_cycle_records']['history']
    text = '''# v52无MOM逐笔诊断与优化方案

日期：2026-09-15。仅研究分析，没有修改策略、回补目标、账户状态或启用实盘。基于固定99日1分钟K线，单边5bp、每委托最低5元的连续回测。

## 一、结论：不是一个问题，而是三个不同环节

1. **买回收紧条件存在逻辑无交集。** 不是参数略不合适，而是在本样本所有31个反T周期中都无法通过收紧分支触发买回。
2. **已经触发买回，但限价未成交后，退出意图没有持续保留。** 策略退回SOLD重新等低价，可能错失已出现的回补机会。
3. **从未触发买回时，可以无限期持有少掉的底仓。** 紧急买回与日末强平关闭、跨日目标不变，3日告警只提醒、不改变行为。额度控制约束了规模，并不约束等待时间或踏空损失。

这比“卖点太低”更准确。卖出确实偏敏感，但两轮关键交易卖出后都出现过回落，问题还在于如何结束做T。

## 二、8月6日这100股：为什么一直没买回

### 1. 卖出依据与实际成交

| 时间/事件 | 当时已知信息 | 策略动作 |
|---|---|---|
| 当日基准 | 开盘310.56，执行阈值322.25，ATR约10.5306% | 主反T允许；日线风格sideways |
| 09:57 | 盘中强度shadow日志S=0.996 | 强度仅观察，不拦截原阈值卖出 |
| 10:00分钟收盘 | 323.00超过322.25，S=1 | 进入SPIKING追踪峰值 |
| 10:01分钟收盘 | 321.86，相对323.00回撤0.353%，大于固定0.1% | 提交卖出100股 |
| 随后的开盘事件 | 321.86 | 订单88全部成交，费用16.093元 |
| 10:05分钟收盘 | 319.01，相对卖价回落0.885% | 没到买回目标，仍SOLD |
| 10:07分钟K线 | 最低318.54，但收盘319.28 | 日内最低价越过99%线，但收盘决策未越线 |
| 10:08分钟收盘 | 321.58 | 未启动回补，回落机会消失 |
| 10:32 | elapsed=31，价格324.95 | 收紧分支计算了更高目标，但无交集问题仍在 |
| 当日收盘 | 328.25 | 没有收盘强制回补 |
| 9月10日期末 | 435.74，仍缺100股 | 相对持有少赚11,388元，非已执行的回购现金亏损 |

分钟收盘和下一条开盘事件可以显示同一个墙钟时间，例如10:01收盘之后的10:01开盘事件；事件序号不同，成交严格晚于提交。不能据同时间标签认定使用未来价格。

### 2. 三条回补线

```text
卖出均价 P = 321.86
ATR A = 10.530626%
原目标 = round(P × (1 − A × 0.15), 2) = 316.78
收紧目标 = round(P × (1 − A × 0.15 × 0.60), 2) = 318.81
99%回升线 = round(P × 0.99, 2) = 318.64
收紧分支启用价格条件 = 当前价 > P × 0.995 = 320.2507
```

原目标要求回落约1.58%。实际卖出后的最低分钟收盘仅319.01；全程最低分钟内低价318.54也未到原目标316.78。

99%规则要先观察到价格向下越过318.64，再向上回到该线。分钟收盘最低319.01没有满足第一步。原始逐笔行情或许看到了318.54，但本回测只在分钟收盘决策，不能把分钟内低点当成当时已经确认的回升买回，也不能假设能在低点成交。

从订单88至样本结束，没有新的买回委托；跟踪中状态持续SOLD、没有暂停原因。因此本轮不是现金不足、拒单或连接故障造成的未成交，而是没有生成退出委托。

### 3. “收紧”为什么形同虚设

代码先判断 `price > P×0.995` 才给局部变量设置收紧目标，随后才判断 `price <= tightened_target`。

这轮需要同时满足：**价格高于320.25，又不高于318.81**，不可能。

价格以后降下来时，局部变量每次重新从原目标316.78开始，收紧结果没有锁存。不是“先收紧，以后就沿用收紧价”。除分币舍入边界外，当 `ATR×0.15×0.60 >= 0.5%`，即ATR约大于等于5.56%，收紧条件即无交集。本样本31个反T周期ATR在6.24%～12.75%，全部处于该区间。

此外 `sell_elapsed_bars > 30` 是循环计数，不是明确的30分钟。在此1分钟回测约对应31个有效循环；实盘轮询频率不同，时间含义也不同。这是另一个应分离修正的口径问题。

**不能宣称修复收紧条件就能救回本轮：** 最低收盘319.01仍高于收紧价318.81；即使锁存正确，也未必成交。

## 三、6月2日至7月13日：有机会回补，却因限价失败后重新等待

| 事件 | 证据 | 结果 |
|---|---|---|
| 6月2日09:40，订单48 | 卖出100股，实际均价385.00 | 原买回目标380.89 |
| 09:52 | 分钟收盘380.69，低于目标 | SOLD进入DIPPING |
| 09:54，订单49 | 回升到382.60，提交限价382.60买回 | 下一开盘不满足限价，未成交并撤销 |
| 09:54:08.5 | NORMAL-FAIL，退回SOLD | 不持续执行已确认的退出意图，重新等目标 |
| 期间最高分钟收盘 | 6月25日599.85 | 100股最大观察踏空21,485元（非最终亏损） |
| 7月13日 | 订单50～53仍遇限价未满足；订单54最终384.72成交 | 毛赚28元，费用38.486元，净亏10.486元 |

这轮持续29个交易日。只看毛收益会被记为一次“胜利”，但代价是漫长等待和巨大中途踏空，扣费还亏损。

注意：当时的限价不成交是所选分钟撮合模型的结果，不能保证真实逐笔环境同样不成交；也不能反过来假设同分钟提交就立刻成交。

## 四、其他逐笔结论

全样本完成36个周期，30个扣费后盈利；主反T30轮、主正T6轮，另有1个未完成反T。主反T已完成毛收益15,031元，主正T毛亏204元。未完成反T估值−11,388元，全部费用1,321.6825元，最终相对持有净增量2,117.3175元。

有9个已完成周期跨交易日；其中也有等待后获利较好的周期，所以“隔夜一律禁止”不是已经被本次证据支持的结论。逐轮明细附后，不删除亏损或长等待样本。

## 五、优化方案：先修退出流程，再试少量策略候选

### A. 优先修复已有收紧机制，不改变首轮卖点

- 用连续有效交易分钟计时，午休不累计；重启保存已累计时长，不能从进程重启重新计时。
- 收紧是否生效只取决于明确时间条件，移除与目标不相容的即时价格条件；一旦生效，将有效目标锁存到所属周期，跨日保持。
- 首次可保留当前0.15×0.60，单独验证修复收益，不同时改卖点、仓位或强度过滤。
- 日志同时显示原目标、有效目标、计时时长和当前状态，避免HB仍只显示原目标。
- 验收：本轮ATR条件下收紧目标可以真正触发，但不得事后声称319.01已触到318.81。

### B. 已确认回补后，保留退出意图并有界重试

新增独立 `EXIT_PENDING` 状态：买回信号出现后，不因一次限价失败退回重新等低价；记录首次触发时刻、价格、剩余股数和订单归属。

- 每股仍串行处理订单，先确认原单撤销/终态及累计成交，再考虑重报；只重报剩余数量，避免重复买入。
- 每个新完整分钟更新一次限价，不能在同一分钟无限追价；候选最多3次更新。
- 候选追价上限 `首次退出触发价 × (1 + 入场ATR × 0.05)`；达到上限或超时，输出明确退出升级告警，而非静默遗忘退出意图。这里只提出候选，不表示已验证最优。
- 升级后是否允许突破价格上限接受亏损，应作为单独风险策略明确决定；不会在实现重试时偷偷变成无上限市价追买。
- 这能针对订单49类型问题，但不能解决订单88之后从未触发的情况。

### C. 已出现小幅有利回落时，增加回升退出候选

不要让“差一点到大目标”成为整轮永久不退出的理由。候选只改退出：

```text
P = 该周期实际卖出均价；A = 开仓时ATR比例
L = 卖出后已完成分钟收盘价的最低值
回落达到 A×k 后，启用低点回升监控
之后价格较L反弹达到原有0.1%时，进入EXIT_PENDING
k只研究0.03、0.05、0.08三档；未选定默认上线值
原316.78类型目标仍可先触发；不存在“选更有利的未来成交价”
```

这与原99%规则的区别是：回落启动幅度与波动相关，且完全使用当时已知分钟收盘，不借用分钟内低点。会更早结束一些好交易，可能减少单轮利润、增加再入场费用，必须重跑共享现金和次数路径。**不能只拿8月6日319元附近这一段证明收益改善。** 这不是按原持仓成本设保护线，也不承诺保本。

### D. 防止长时间踏空：作为独立风险决策，不夹带实施

若仍坚持任何时候都不接受亏损回补，反T可能永远买不回来。仅改参数无法同时保证不亏、完整回补和不错过上涨。

可研究“相对卖价上涨达到入场ATR×0.25/0.50，且连续3个完整分钟满足”时触发风险退出；只测试这两档，不围绕本次最低点搜索。它可能在震荡中反复止损，隔夜跳空也可能超过预算，不保证最大损失受控。

另一条不同路线是反T新开仓前加入持续上行准入限制：本轮卖出前shadow强度已达1，可先测试S≥0.8持续3分钟时暂停新反T，恢复需重新形成信号。但这会改变开仓样本，不能与A/B/C同时开启后笼统归功于某一个改动。

这两项都属于新的风险取舍，尤其亏损回补与原“隔夜等待、不新增到期强制止损”的边界不同，**需明确选择后才能实施**。目前只提出选项，不默认启用。

## 六、验证顺序与成功标准

1. A单独修复；B单独有界重试；C三档单独消融，再评估必要组合。暂不调其他参数。
2. 委托仍最早下一开盘事件、同样费用与成交量上限；未成交原单未确认终态不得重发。
3. 报告相对持有净增量、回撤、未回补最大估值损失、持有交易日分布、资金占用、扣费胜率和未完成数量，不能只看完成率。
4. 6月2日与8月6日是回归案例，不是优化目标；固定99日已经看过，不能再称后30日严格样本外。后续新增数据才能提供独立证据。
5. 成功须同时看收益与风险；减少等待但大幅增加亏损退出，不算自动成功。未来若实施，先研究版与影子观察，不实盘验证。

## 七、证据、限制与复现

- 本次插入只读跟踪重新回放，逐笔trades、orders、cycles、未完成unclosed及每分钟equity与正式结果逐项完全一致；不是重算出一份有利的新收益。
- 当前工作区的加载器v39分支和无MOM注释与9月14日结果哈希不同，已记录原始及当前哈希；v52运行行为以全量结果相等校验确认。未改动这些策略源码。
- [逐分钟状态及完整日志](v52_nomom_trace_20260915/trace.json)、[跟踪脚本](trace_v52_nomom.py)、[正式回测结果](nomom_20260914/verified/v52_nomom_5bp.json)。
- 关键源码：[卖出及回补](../Stragety/MiniQMT_Stragety/DayT/DayT_v52_nomom.py)、[公共开关参数](../Stragety/MiniQMT_Stragety/core/config.py)。重点方法`_handle_spiking`、`_handle_sold`、`_do_buyback`、`_rollover_cycle_day`。
- 按signal-validation技能检查了费用、阶段及胜率偏差；未使用无关日线信号替代分钟交易验证。所有优化公式均是未验证候选，而非收益结论。

## 附录A：全部已完成周期（按实际成交归属）

净收益为该周期毛收益减实际模拟订单费用。交易日数含开、平仓当天；分钟收盘观察到的最大不利价差不等于实盘逐笔极值或最终亏损。

|序号|方向|开仓时间|平仓时间|开仓均价|数量|交易日|毛收益|费用|净收益|最大不利价差估值|
|---|---|---|---|---:|---:|---:|---:|---:|---:|---:|
'''
    dates = sorted({e['time'][:10] for e in result['equity']})
    for i, (h, c) in enumerate(zip(history, result['cycles']), 1):
        assert abs(h['realized_gross'] - c['gross']) < 1e-6
        price = h['opening_value'] / h['opening_quantity']
        observations = minute.loc[(minute.stamp > pd.Timestamp(c['opened'])) & (minute.stamp <= pd.Timestamp(c['closed'])), 'close']
        adverse = max(0., (observations.max() - price) if h['direction'] == 'SHORT' else (price - observations.min())) * h['opening_quantity']
        age = sum(c['opened'][:10] <= d <= c['closed'][:10] for d in dates)
        text += f"|{i}|{h['direction']}|{c['opened']}|{c['closed']}|{price:.2f}|{h['opening_quantity']}|{age}|{c['gross']:.2f}|{h['fees']:.2f}|{c['gross']-h['fees']:.2f}|{adverse:.2f}|\n"
    text += '\n## 附录B：全部委托（含未成交）\n\n提交用事件序号标识；成交时间为对应实际模拟fill。无成交均价和时间显示—；54为撤单终态，56为全部成交。\n\n|订单|方向/原因|提交事件|限价|成交股数|成交均价|首次成交时间|状态|费用|受阻原因|\n|---|---|---:|---:|---:|---:|---|---:|---:|---|\n'
    for o in result['orders']:
        fills = [t for t in result['trades'] if t['order'] == o['order_id']]
        avg = f"{o['traded_value']/o['traded_volume']:.2f}" if o['traded_volume'] else '—'
        text += f"|{o['order_id']}|{o['label']}|{o['submitted_event']}|{o['limit'] if o['limit'] else '下一开盘'}|{o['traded_volume']}|{avg}|{fills[0]['time'] if fills else '—'}|{o['order_status']}|{o['fee']:.3f}|{o['reason']}|\n"
    target = ROOT / 'analysis/DayT_v52_noMOM_trade_diagnosis_plan.md'
    target.write_text(text, encoding='utf-8')
    print('REPORT:', target, 'cycles', len(history), 'orders', len(result['orders']))


if __name__ == '__main__':
    main()
